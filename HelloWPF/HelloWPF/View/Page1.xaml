<Page x:Class="HelloWPF.View.Page1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="Page1">

    <Grid>
        <Button Content="Button" HorizontalAlignment="Left" Width="75" Margin="0,0,0,281" Command="{Binding ClickedCommand}"/>
        <TextBox HorizontalAlignment="Left" Height="23" Margin="63,99,0,0" TextWrapping="Wrap" Text="{Binding Text1}" VerticalAlignment="Top" Width="120" />
        <Button Content="Button" HorizontalAlignment="Left" Height="40" Margin="75,164,0,0" VerticalAlignment="Top" Width="82" Command="{Binding Clickedplusgmail}"/>

    </Grid>
</Page>
